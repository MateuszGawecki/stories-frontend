import React  from "react";

import Navbar from "../nav/Navbar";

const Settings = () => {
    return (
        <div>
            <Navbar/>
            <p>Settings!</p>
        </div>
    );
};

export default Settings;